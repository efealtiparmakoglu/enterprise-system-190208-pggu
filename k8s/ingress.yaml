apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-190208-pggu-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-190208-pggu.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-190208-pggu-service
            port:
              number: 80
